import React, { Component } from 'react';
import { Section, Background, Text } from '../components/common';

class ProfileScreen extends Component {
  render() {
    <Background>
      <Section>
        <Text style={{color: 'white'}}>
          PROFILE
        </Text>
      </Section>
    </Background>
  }
}

export default ProfileScreen;
